%h1 Listing phases

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @phases.each do |phase|
      %tr
        %td= link_to 'Edit', edit_phase_path(phase)
        %td= link_to 'Destroy', phase, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Phase', new_tournament_phase_path(@tournament)
